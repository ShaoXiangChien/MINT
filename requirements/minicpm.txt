# Requirements for MiniCPM-V 2.6 (8B)
# Install with: pip install -r requirements/minicpm.txt
#
# VRAM: ~16 GB (float16)
# Model: openbmb/MiniCPM-V-2_6
#
# NOTE: MiniCPM-V uses trust_remote_code=True and ships custom modeling files.
# The modeling code is downloaded automatically from HuggingFace Hub.

-r base.txt

transformers>=4.40.0,<5.0.0
accelerate>=0.26.0

# Required by MiniCPM-V's custom modeling code
timm>=0.9.0
torchvision>=0.16.0
sentencepiece>=0.1.99
